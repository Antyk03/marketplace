<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Connessione al DB H2 in memoria -->
        <property name="hibernate.connection.driver_class">org.h2.Driver</property>
        <property name="hibernate.connection.url">${db.url}</property>
        <property name="hibernate.connection.username">${db.username}</property>
        <property name="hibernate.connection.password">${db.password}</property>
        <property name="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mostra SQL sulla console -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Aggiorna automaticamente schema DB -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Classi annotate -->
        <mapping class="antyk03.marketplaceserver.modello.Utente"/>
        <mapping class="antyk03.marketplaceserver.modello.DatiUtente"/>
        <mapping class="antyk03.marketplaceserver.modello.Prodotto"/>
        <mapping class="antyk03.marketplaceserver.modello.Ordine"/>
        <mapping class="antyk03.marketplaceserver.modello.ProdottoOrdine"/>
    </session-factory>
</hibernate-configuration>
